# Defect-Class Registry

Created 2026-09-14 by the coordinator on adopting `docs/META_REVIEW_2026-09-14_ITER10-12.md` §5.4
(governance artifact, AUTONOMY §1a). It is the data source for the **Rc (recurrence)** term of scoring
model v2 and for selection rule **S4 (gate-or-waiver)**: any class with ≥ 2 dated occurrences must carry
either a mechanical gate (proven with a planted probe) or a dated waiver with an owner.

**Evidence standard.** An occurrence counts only with a dated, checkable reference. Rows marked
*(per meta-review, not re-verified)* are carried from the meta-review and must be re-verified before
they are used to award Rc = +2.

| ID | Defect class | Dated occurrences (evidence) | Count | Gate | Waiver | Status |
|---|---|---|---:|---|---|---|
| DC-01 | **Hard-coded catalogue counts drift from data** | It. 6 2026-06-18 (1,155/1,156/1,160 unified); commits `0a596d5b` 2026-09-09 (robotics "50" vs 92), `c0c64833` 2026-09-10 (ai-labs counts); It. 11 2026-09-14 (llms.txt "1,260+"); It. 12 2026-09-14 (~20 literals, 11 routes). It. 8 agent-introduced literal *(per meta-review, not re-verified)* | ≥ 5 | `site/scripts/test-no-stale-counts.mjs` (It. 12; scans `src/app` + `src/components` only) | — | **Gated, partial coverage.** Not covered: `site/scripts/` generators, `CLAUDE.md` data notes |
| DC-02 | **Published method copy contradicts the canonical formula** | It. 9 2026-06-20 ("base /80" model on /methodology); It. 10 2026-09-14 ("balanced beats spiky" on /ai-models/methodology). **3rd site CONFIRMED 2026-09-16 (It. 15)** — the "probable G-3" guess was right: `methodology/page.tsx` (~line 1294), `ConsistencyStepChart.tsx` and `dimensions.ts:635` each published a false claim. Coordinator computed against `scoring.mjs`: (a) two of four consistency steps are unreachable — σ across 8 dimensions bounded 0–5 cannot exceed **2.500**, so "σ 3.0–5.0 → 0.4" and "σ > 5.0 → 0.1" can never fire; (b) "a balanced 70/70 profile beats a spiky 90/40 profile" is false in general — [3.8×8] = 70.0 loses to [5,5,5,5,2.6,2.6,2.6,2.6] = 72.0; (c) `IntegrationPremiumDiagram` published an impossible premium of 0.5 (reachable set is exactly {0, 1.5, 2, 3, 4, 4.5, 6, 8, 10}) | **3** | **Gated 2026-09-16 (It. 15):** `site/scripts/test-method-claims.mjs`, wired into `npm run test` as `test:method-claims` (chain now 23 steps). Computes against `scoring.mjs` — asserts the reachable premium set and the σ ceiling, fails on any published step or premium value that cannot occur, and recomputes every worked example's σ/base/premium/composite. The charts and the test import shared constants (`consistencyStepsData.ts`, `integrationPremiumExamples.ts`) so copy and data cannot drift | — | **Gated.** Proven by a coordinator-run probe: flipping an unreachable step to `reachable: true` fails by name, restoring passes 33/33, file byte-identical after. It checks numbers, not prose — string-matching is precisely what let this class recur three times |
| DC-03 | **Briefing narrates an unapplied proposal as a published score movement** | Verified by coordinator 2026-09-14, all with `pipeline.scoreChangesApplied: 0`: `2026-07-28.json` ("Taipei's Score Fell 8 Points"), `2026-07-30.json` ("Portugal's face-covering ban cuts its score 5 points"; applied 2026-08-16), `2026-08-26.json` ("Spain fell 9.4 points", "Abbott Laboratories fell 7.8 points"), `2026-09-01.json` (Chile −20, Regions Financial −38.4, Kenya −5.9, Starbucks −5.3), `2026-09-14.json` ("Hong Kong falls 5.9 points"; applied later that day). Earlier May–July matches surfaced by the It. 13 report-only scan not yet individually classified | ≥ 5 cycles | In progress: It. 13 (D-1) `unapplied-score-movement` lint rule, forward-dated from 2026-09-15. **First implementation failed validation** (≈ half of ~60 historical matches were false positives, e.g. "death toll rose to 3,899", "fuel prices rose"); rework in progress binding the verb to the score | — | **Gated (It. 13, validated 2026-09-14; uncommitted pending founder).** Rule `unapplied-score-movement` in `site/scripts/lib/lint-rules.mjs`, forward-dated ≥ 2026-09-15; negative control proven on forward-dated real briefings; 30+9 real/probe fixtures. Limitation: aggregate `scoreChangesApplied`, not per-entity |
| DC-04 | **Briefing claim contradicts its sources** (RISK-020) | 2026-09-14 cycle: four errors caught only by coordinator review (commit `cef937df` message). **2026-09-15 cycle: 12 errors in the drafted public briefing** ("Oracle at the bottom of the benchmark", "last night's briefing" for a scan-only drop, an "unconflicted" prior review, a false integration-bonus rule, an unsourced Sweden adjustment history) — all passed `validate-daily-briefings` and `lint-daily-briefings`, caught by coordinator claim-to-source review and corrected before commit (ITERATION_LOG research 2026-09-15). **2026-09-17 cycle, the first briefing live-enforced by the It. 16 gate:** the draft passed `lint-daily-briefings` (claim-to-source 0 violations) while carrying an invented relationship ("a sister AI lab, Imbue"), a 2025 event framed as overnight news, a misstated sourcing rule, and **5 `sourceTier` values contradicting the cited assessments** (4 news outlets inflated tier 2 → 4; the UN High Commissioner deflated 4 → 2). All were caught by coordinator review and corrected before commit. **The gate does not check evidence tier or event recency.** | **3 cycles** | **Gated 2026-09-16 (It. 16):** `claim-to-source` in `site/scripts/lib/lint-rules.mjs`, wired into `lint-daily-briefings.mjs` and `npm run test`. Four checks, forward-dated to 2026-09-17 so no published briefing is retro-failed (§1c): superlative/rank claims vs the published index (extremity tested as composite tied with index min/max, not `rank === 1`, because 12 countries tie at composite 0); numbers predicated of an entity vs that briefing's own `recentAssessments` or the index composite; "last night's briefing said X" vs that date's file; formula statements in `methodologyNotes[]` recomputed against `scoring.mjs` | — | **Gated.** Coordinator-verified: the real Oracle claim flags by name ("ranks 413 of 447"), Wellington's wrong 84.0 flags, a false 12-point bonus cap flags; corrected/hedged wordings pass. Two defects fixed before commit — number misattribution (28 false positives → 0, by binding each number to the entity it is predicated of and excluding band edges, deltas, transition "from" legs, formula components and 0–5 dimension values; no phrase suppressions, no residuals), and **silent degradation**: a wrong indexes path returned an empty lookup so every check degraded to advisory while the run reported success — it now throws and the linter exits 2 |
| DC-05 | **Entity identity / slug defects** (duplicates, collisions, accent folding, truncated names) | RISK-003 (Cabo Verde/Cape Verde, São Tomé duplicates; DR Congo truncation) resolved 2026-09-14 `7dfa27a3`; RISK-017 18 cross-index collisions (Phoenix fixed 2026-09-14); RISK-018 13 accented-slug mismatches; commit `70f81dc2` (26 orphaned records); **2026-09-15: 20 Fortune 500 names stored with HTML entities since `a60208d9` (2026-04-14), producing three disagreeing slugs per company and a live visible "&amp;" (RISK-023)** | ≥ 5 | **Gated 2026-09-16 (It. 14):** `test-entity-records.mjs` (19,687 assertions) wired into `npm run test`, which CI runs before every deploy; `export-public-data.mjs` now fails the build on any cross-index slug collision not in the dated `site/scripts/known-collisions.json`, and on any listed entry that no longer occurs (shrink-only). Proven by a planted 17th collision | — | **Gated for new occurrences.** The 20 encoded names (RISK-023) are migrated. Of the 16 existing collisions, **1 is fixed** (It. 19, 2026-09-16: `singapore-global-cities` pinned, bare `singapore` now serves the country; ratchet 16 → 15, warnings 64 → 63). **15 remain:** 13 US-city twins and `washington-dc` are mechanical; `figure-ai` and `1x-technologies` are **not a naming fix** and are blocked on D-13 (proposed, unratified) — pinning a slug per composite would entrench a duplicate publication of one company. Coordinator pinned and reverted those two in It. 19; every gate passed green throughout, because **no gate encodes an unratified ownership decision**. **Link-integrity variant gated 2026-09-17 (It. 20):** a pinned slug is only useful if renderers honour it — four components (`RankingTable`, `IndexPageCharts`, `EntitySearch`, `NavbarSearch`) re-derived slugs from entity names, so 34 links (77 in `IndexPageCharts`, which also carried a private naive slugger) resolved only via nginx rewrites. Occurrences: 2026-09-01 (26 entities, record at declared slug vs page at `slugify(name)` — see `entities.ts:246-250`, and the ~15 surviving "Slug-override corrections" rewrites), Phoenix 2026-09-14, Singapore 2026-09-16, measured live 2026-09-17. Fixed by exporting one `rowSlug()` from `src/lib/slugify.ts`; gated by `site/scripts/test-pinned-slugs.mjs` in `npm run test`, which asserts fixture non-vacuity (≥ 1 divergent row plus 2 named anchors; 34 rows at time of writing) and scans `src/app` + `src/components` with **no allowlist and no proximity heuristic**. Proven by a planted probe: a bare `slugify(entry.name)` failed by file:line, and the file was sha256-identical after removal. **Committed `119f1757` and deployed 2026-09-17; verified live 1,323/1,323 ranking-page entity hrefs → 200.** **2026-09-17 (It. 23):** the accented-slug half of this class (RISK-018; 14 of 16 non-ASCII names; `/data/scores/sao-paulo.json` -> `/404` live) is now **gated and ratcheted** by `test:slug-conventions` plus `known-slug-divergences.json`, and the 12 duplicate slug implementations that produced it are one module. The divergence is frozen, not fixed — remediation row RS-4b filed in the same loop. **Known hole 3:** `generate-newsletter-html.mjs` carries two inline slug expressions in a third variant, outside the scan. **Known hole 2 (Meta-review 3, V9a):** the test checks a private copy of `rowSlug`, not the shipped export, so a broken shipped function still passes. **Known hole:** the scan does not cover `src/lib` or `src/data`, where a fifth copy of the rule still lives in `entities.ts` |
| DC-06 | **Date / year errors in evidence** (including false drops) | 2026-09-01 briefing (2023 UN finding cited as 2026); 2026-09-14 digest (France protests dated 2025, not 2026); 2026-08-01 (two Kyiv strikes a year apart) — per briefing headlines/digest. **Reverse direction, verified by coordinator 2026-09-15:** `research/scans/2026-09-14.json` dropped a real Lesotho AGOA finding as "misdated and substantively wrong", conflating the February 2026 extension (to end-2026) with the 2 September 2026 Continuing Appropriations and Extensions Act, 2027 (to 2028) — Sunday Times, published 2026-09-04. Over-correction is the same class: date discipline must verify, not presume error | ≥ 4 | Partial: `validate-scan.mjs` date checks *(per meta-review, not re-verified)* | — | Partly gated |
| DC-07 | **Currency / unit errors** | *(per meta-review earlier review F4, not re-verified)* | ? | none | — | Needs evidence before scoring |
| DC-09 | **A gate reports FAIL on valid evidence, so its red status is ignored** (miscalibrated guard) | `research/scripts/validate-rotation-state.mjs` — 22 blocking FAILs ("zero-evidence") carried as known since at least `7dfa27a3` (2026-09-14, "22 failures (was 23)"). Coordinator classification 2026-09-15: **0 true phantoms** — 20 have a same-date `research/change-proposals/<slug>-<date>.json` plus a digest entry (early recording convention, 2026-04-29 → 05-09); 1 (Procter & Gamble) has `research/assessments/procter-gamble-2026-06-12.md` under a different slug than its rotation key `procter-amp-gamble`; 1 (Côte d'Ivoire, key `c-te-divoire`) has a 2026-06-24 digest entry only. Related: `validate-scan.mjs 2026-09-14` FAILs on 1,331 vs 1,329 after the structural merge. Earlier instance of the "dead guard" pattern: product-separation validator wired to nothing (commit message `93611974` era) *(per meta-review, not re-verified)* | ≥ 2 | **Gated 2026-09-16 (RS-1):** `validate-rotation-state.mjs` escalates a missing exact report through derived alias slugs, a same-date ±1-day change proposal, and a digest mention, downgrading to a WARN that names the evidence class; FAIL is reserved for entities with none of the three. 25 FAIL → 0 FAIL / 25 WARN. Wired into `npm run test` as `test:rotation-state` so it cannot rot unnoticed | — | **Gated.** Coordinator negative controls confirm it still fails on no evidence, unrelated files, an alias report on the wrong date, a proposal outside ±1 day, and a digest that does not name the entity |
| DC-08 | **Build writes tracked files non-deterministically** | `site/scripts/build-special-briefings.mjs:474` `generatedAt: new Date().toISOString()` — 16 tracked JSON files changed timestamp-only after the 2026-09-14 builds (coordinator verified) | 1 class, every build (19 tracked files per build) | **Fixed 2026-09-16 (It. 18):** generated artifacts no longer stamp wall-clock time. `generatedAt` per special briefing is derived from the source `.md`'s git commit date (front-matter date as fallback), so it changes only when the content actually changes; the unread `updatedAt` fields in `special-briefings/manifest.json` and `updates/manifest.json` were deleted outright after grepping for readers; `build-manifest.json` is untracked (it is a genuine build artifact, still generated and served) | — | **Fixed.** Coordinator proof: two consecutive generator runs leave **0 dirty paths** and `manifest.json` byte-identical. Churn 19 → 0. Held content contained — only `america-at-250-*.json` shows content changes (it rebuilds from the held `.md` rewrite) and is excluded from commits with its source, per §1c |

| DC-10 | **A hand-authored JSON edit introduces duplicate keys, silently nulling the earlier value** | 2026-09-16: the coordinator inserted `reviewed_by` / `reviewed_date` / `decision` after `status` in `research/change-proposals/wellington-2026-09-15.json` without checking that the file already declared those three keys (as `null`) further down. JSON last-key-wins meant the approval fields parsed as `null` at apply time; only the single, non-duplicated `status: "approved"` was intact. Caught by `score-updater`, which refused to accept the instruction's characterisation of the file, applied on the `status` gate alone, disclosed the contradiction in `APPLIED_CHANGES.md` / `PENDING_CHANGES.md`, and repaired the keys | 1 | none | — | **Ungated (first occurrence).** Prevention: edit structured records through a script that parses, mutates and re-serialises (so a duplicate key is impossible), or grep the key before inserting. A second occurrence requires a mechanical check per S4 — e.g. a JSON duplicate-key linter over `research/change-proposals/**` in `npm run test` |
| DC-11 | **Production runs a different config than the one being edited** (two nginx configs; the image ships `nginx.conf`, `deploy.sh` runtime-copies `nginx-ssl.conf`, and a CI rebuild silently reverts that copy) | 2026-09-01: the "Slug-override corrections" block was added to `nginx-ssl.conf` only — **26 rewrites** (25 robotics-lab legal-name slugs + `/us-state/georgia`) never reached production. 2026-09-16 (RISK-022): the security-header mitigation was written into `nginx-ssl.conf`, i.e. the unshipped file. 2026-09-17 (It. 20 V1): the coordinator asserted nginx 301s rescued naive-slug links; live checks showed 301 → `/404`, because those rewrites were in the unshipped file | **3** | **Gated 2026-09-17 (It. 21):** `site/scripts/test-nginx-redirect-parity.mjs` in `npm run test` (chain 28 → 29) requires `nginx.conf` to be a superset of `nginx-ssl.conf`'s rewrites and fails on a zero parse; new CI job `nginx-config-syntax` runs `nginx -t` on the shipped file and gates `deploy`; the `verify` job sweeps 29 legacy URLs plus a negative control. Coordinator-verified with an independent planted probe (removed the `skydio-inc` rewrite → failed naming `nginx-ssl.conf:99`; restored sha256-identical) | — | **Gated, two configs still exist.** LC-1b (delete `nginx-ssl.conf`, drop the `deploy.sh` copy line) is founder-gated; the parity gate is what keeps them from diverging meanwhile |
| DC-12 | **A published label contradicts the documented scale it displays** (presentation layer diverges from the data convention it renders) | 2026-09-18: `briefing/evidence/index.tsx:34-40` maps evidence tier 1 to "Gov/Court" and 5 to "Trade/Advocacy", inverting `overnight-assessor.md:206` (5 = government/court, 1 = trade press) and `overnight-digest.md:452` ("Tier 5 — strongest evidence"). Live on every briefing page: a Boston.com article renders as "Tier 2 · UN/IO". Found by coordinator deploy verification, not by any gate | **1** | **Gated 2026-09-21 (It. 26):** `test:evidence-tier-labels` (chain 32 → 33) asserts `TIER_LABELS`, `TIER_SHORT_LABELS` and `TIER_COLORS` against the documented scale in both directions, pins the reliability cutoff, and fails if a second `Record<number, …>` tier map appears anywhere in `src/`. Coordinator-verified with an independent planted probe on a different target (the cutoff constant) | — | **Gated; labels corrected, data not migrated.** The survey found the corpus itself split — 18 cycles documented-convention, 19 inverted, 17 self-contradictory — so badges are suppressed before 2026-09-17 rather than the record being rewritten (§1c). Migration is founder option A |
| DC-13 | **The nightly scanner re-surfaces claims already verified false in an earlier cycle** (no memory of debunked items between cycles) | Meta "8,000 layoffs" (true date 2026-05-20): dropped 2026-09-17, dropped again 2026-09-18 (the 09-18 scan record itself calls it "third cycle this has surfaced"). China "Ethnic Unity Law" (passed 2026-03-12): dropped 2026-09-15, dropped again 2026-09-17. Same shape, first occurrence 09-18: PayPal DEI settlement (May 12), Qatar strike (Sept **2025**), Freeport-McMoRan Grasberg (Sept 2025), Cambridge injunction (March 2025) — 8 of 14 drops that cycle were misdated events | **≥ 4** | **Gated 2026-09-20 (It. 25):** `research/known-misdated-claims.json` (10 dated entries, append-only `occurrences[]`) + a check in `validate-scan.mjs` that fails a scan whose `top_entities` matches an active entry, + `test:known-misdated-claims` (76 assertions) in `npm test` (chain 31 → 32). Coordinator-verified with an independent planted probe **and** an over-matching counter-test (a genuine item on the same entity passes) | — | **Gated, partial coverage.** First live run (09-20) saved verification on PayPal but four same-shape claims still had no entry (SC-1b), and the matcher reads narrative prose as well as claims (SC-1c) |
| DC-14 | **An agent runs a destructive git command over another agent's uncommitted work** | 2026-09-18 (INC-009): `overnight-assessor` misread the scanner's uncommitted 2,673-line `rotation-state.json` write as a reformat and ran `git checkout`, discarding all 1,329 `last_scanned`/`last_evidence_touch` values from the same cycle; self-disclosed and reconstructed deterministically, coordinator-verified equivalent (4 changed fields, 0 forbidden) | **1** | **None yet.** Briefs now prohibit `git checkout/restore/stash/reset/clean` for agents touching shared stores (coordinator checklist), but nothing mechanical prevents it | — | **Open — backlog GI-1** (pre-flight snapshot of cycle stores to a gitignored path, so recovery never depends on re-derivability) |
| DC-15 | **A gate compares two artifacts instead of specifying the truth, so it passes when both are wrong** | 2026-09-17 (Meta-review 3): `test:pinned-slugs` tested a private copy of `rowSlug`, not the shipped export — a broken shipped function still passed. **2026-09-21 (Meta-review 4, coordinator-verified):** `test:nginx-redirect-parity` asserts `nginx.conf` is a superset of `nginx-ssl.conf` — delete the same rewrite from **both** files and it reports **4 passed / 0 failed, exit 0**. My own approval probe deleted from one file only, which is exactly why I missed it. Its truth source is also the file LC-1b intends to delete | **2** | **Partial.** Both gates exist and catch single-sided damage; neither encodes the requirement independently | — | **Open — backlog V9d-1.** A gate must compare the artifact against a **specification** (a committed list of the 26 legacy URLs and their targets), not against a second copy that can rot in parallel |
| DC-16 | **Work ships carrying an iteration number that has no entry in `ITERATION_LOG.md`**, so the loop's own record falls behind the code and docs that cite it | **2026-09-23:** commit `9ce57aa3`'s message says *"Record Iterations 26-29"*; its diff adds **26 and 27 only** (`git show 9ce57aa3 -- ITERATION_LOG.md` → two `+## Iteration` lines). Nothing anywhere held 28 or 29. **2026-09-24:** `tools/cb-probe/README.md:372` ("Added in Iteration 32") and `tools/cb-probe/CHANGELOG.md:12` ("Iteration 33") were committed and pushed while `ITERATION_LOG.md` ended at 27 — six iterations (27b, 28–33) unlogged, found during the founder's "GitHub and Desktop" step, by no gate | **2** | **Gated 2026-09-24 (It. 34):** `research/scripts/test-iteration-log-coverage.mjs`, wired into `npm run test` as `test:iteration-log-coverage` (chain 33 → 34). **(A)** every `Iteration N` / `It. N` reference in a tracked text file must resolve to a `## Iteration N` heading; **(B)** the logged sequence must have no hole below its own maximum — the footprint this class actually leaves, and what a false "Record Iterations 26-29" would have failed on; **(C)** non-vacuity floors (35 headings, 650 references, 28 files) plus three named positive controls, two long-form and one abbreviated, so a broken pattern exits `VACUOUS` instead of green (**V8**) | — | **Gated, with one stated hole.** Four coordinator negative controls: a renamed heading fails naming the hole at 25; a planted reference to a non-existent number fails naming number **and** file:line; breaking either the reference regex **or** the heading parse exits `VACUOUS`. All four restored sha256-identical, and probes 3–4 throw *"probe void"* if the line they mean to break is absent. The gate found a defect in itself on first run — the abbreviated branch matched the ordinary word **"Its"** ("Its 40 subdimension scores", "Its 2017 Refugee Law"), 10 false dangling references, repaired by requiring the period rather than allowlisting ten research files. **Hole: commit messages are not tracked files and cannot be linted after the fact**, which is exactly where occurrence 1 lived; check B is the substitute. **Practice rule:** a commit message asserting a record must name the file the record is in, and that file must be in the same pathspec |

## Change log
- 2026-09-24 — DC-16 added (It. 34), gate recorded and probed; its first occurrence (2026-09-23) is a commit message that claimed a record its diff did not contain.
- 2026-09-17 — DC-11 added (It. 21), gate recorded; DC-04 third occurrence recorded (2026-09-17 briefing).
- 2026-09-14 — created; DC-01, DC-02, DC-03, DC-05, DC-08 verified by coordinator; DC-06/07 carried with flags.
