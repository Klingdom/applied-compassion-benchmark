Subject: Compassion Benchmark — Founder briefing, Mon 2026-09-14: done this week + roadmap

Phil,

Here is today's briefing: what shipped in the last seven days, what I did today, and the plan for this week. Anything that needs your approval is marked [DECISION].

────────────────────────────────────────
1. TODAY (improvement loop, 2026-09-14)
────────────────────────────────────────
• Fixed a false public methodology claim (RISK-019). The /ai-models/methodology FAQ said "a balanced profile scores higher than a spiky one with the same average." Under the canonical formula that is false: [1,1,1,1,5,5,5,5] scores 51.5, while a flat 3.0 scores 50.0. The copy now says what the formula actually does: the dimension average rescaled to 0–100, plus an integration bonus of up to 10 points. The bonus is earned as dimensions reach 4.0, drops by a fifth for each dimension below 4.0, and is zero if any dimension is 0. No formula, data or score changed.
  Validation: every number in the new copy was checked against scoring.mjs, tsc is clean, and all test suites pass (scoring 125/125, model-releases 93/93). The full static build passed (1,978 pages).
• Checked the uncommitted Worker typecheck fix left from an earlier session (FormDataEntryValue → string | null, plus a non-blocking "Worker typecheck" CI job). `npm run typecheck` in worker/ now passes; it had never passed before.
• Neither change is committed or deployed yet. Under AUTONOMY.md §1b that needs your go-ahead. [DECISION: approve commit + deploy]

────────────────────────────────────────
2. COMPLETED THIS WEEK (2026-09-07 → 09-14)
────────────────────────────────────────
AI model benchmark (CB-MODEL)
• Moved the 33-prompt suite into a versioned task bank. Restored the scorer, made the registry executable, and added a statistics layer (uncertainty, agreement, failure rates).
• Repaired 5 defective task items. Both arms of matched-counterfactual items now render.
• Phase A evaluation harness with a replay adapter (no network, no credentials). Added a strict composite entry point that refuses to score missing dimensions.
• Shipped /ai-models as a pre-registration: the method is published and no model has been scored yet (D-29). Added it to the sitemap.
• Release-watch store and validator (wired into test and build), a release-watch section, and bring-your-own-model scoring. D-30 ratified.

Data integrity and site accuracy
• Robotics-labs and AI-labs counts now come from the data. That removed six stale "50" claims; the robotics index actually holds 92. Band vocabulary is unified.
• Separated two published entity names that had been merged by mistake. Corrected a false scan-freshness stamp and relabelled the 09-09 scan to its true coverage.
• Turned on the product-separation guard, with expiring waivers.
• Structural fixes you directed: merged the duplicate Cape Verde / Cabo Verde and São Tomé rows (countries 193 → 191, with 301 redirects), fixed the truncated "Democratic Republic of C" name, and fixed the Phoenix slug collision.

Security
• Closed a live .env leak path at the repo root (F-01, HIGH).
• Worker admin alerts now strip secrets before they leave the edge (F-04).

Research
• 09-14 catch-up cycle closed the 09-02 → 09-13 coverage gap: 1,331 entities scanned, 19 assessed, 1 proposal.
• Hong Kong applied with your approval: 32.8 → 26.9 (Developing), after the Tiananmen-vigil organisers were sentenced under the National Security Law.
• France and Slovenia band crossings were measured but withheld (evidence test failed; mostly placeholder de-seeding).
• In review I caught four factual errors in the draft public briefing and fixed them before publication.

Delivery and planning
• Auto-deploy is working again: 7 successful runs in a row since 09-09, after 53 failures between 07-14 and 09-08.
• PR/FAQ for the AI model benchmark and continuous research, built from 12 specialist reviews. It ranks 24 improvements and lists 9 decisions for you.

────────────────────────────────────────
3. ROADMAP — THIS WEEK (PR/FAQ "Do now")
────────────────────────────────────────
One validated improvement per loop, in this order:
1. Coverage/freshness dashboard (U) generated from rotation-state.json. It answers "how much is actually researched?" with a number: today 61.7% of entities (820 of 1,329) have never been individually assessed.
2. Fix /cite and llms.txt (S). Our citation instructions currently produce dead links, and llms.txt leaves out the AI model program. About half a day.
3. A status ladder separating "measured" from "published/applied/held" (D). Today's headline said "Hong Kong falls 5.9" while the briefing recorded 0 applied changes.
4. Score-Watch disclosure (Q): at minimum, state the operational gap on the product page. See decision A.
5. Waiver cliff (R): make "passed with waivers" visibly different from a clean pass, and warn 30 days before expiry.
Also queued:
• Audit the related "balanced vs spiky" wording on /methodology, ConsistencyStepChart and dimensions.ts. It compares different averages, so it is likely accurate, but it needs verifying.
• Clear RISK-017/018: 16 cross-index slug collisions and 13 accented-slug mismatches, which serve the wrong entity's score file.

────────────────────────────────────────
4. DECISIONS NEEDED FROM YOU (highest urgency first)
────────────────────────────────────────
A. Score-Watch ($79/yr) is sold as LIVE, but api.compassionbenchmark.com does not resolve (NXDOMAIN). The webhook and badge embeds cannot work. Recommendation: pause sales now (useGumroad:false), then deploy the Worker and DNS record and do a real test purchase before re-enabling. Also check Gumroad for any purchases that went unfulfilled.
B. All 6 product-separation waivers expire 2026-12-09. From 12-10 every build and deploy fails. Recommendation: extend them on staggered dates now, then ratify D-13 separately.
C. Approve committing and deploying today's two validated changes (section 1).
D. Approval provenance: signed commits or founder-approved PRs for index and proposal changes. Right now "reviewed_by: founder" is a text field any agent can write.
E. A minimum research cadence ahead of CB-MODEL build work, e.g. at least 5 validated cycles per 7 days. The nightly pipeline has never run unattended.
F. Nonprofit vs commercial messaging must be settled before any external PR/FAQ ships.

────────────────────────────────────────
5. HEALTH SNAPSHOT
────────────────────────────────────────
• Deploy: green (7 in a row). Verification still doesn't check score values (RISK-004).
• Tests: all suites pass. Worker typecheck passes (pending commit).
• Open high risks: Score-Watch host (014), waiver cliff (015), unattended pipeline (016), slug collisions (017/018), briefing fact-check gap (020), approval provenance (021).
• Majority of scores are still placeholders: 61.7% never individually assessed. The de-seeding programme continues.

────────────────────────────────────────
ADDENDUM — Iteration 11 (later on 2026-09-14)
────────────────────────────────────────
• Fixed the dead citation pattern (roadmap item 2, first half). /cite, /media and /data told readers to cite compassionbenchmark.com/fortune-500/microsoft. On production that lands on a 404 page, and the 404 page itself returns HTTP 200. All three now teach /company/microsoft and the [entity-type]/[slug] pattern; /cite also shows a table of URL prefixes generated from the index registry.
• llms.txt now takes its entity count from the data: 1,325, where it previously said a stale "1,260+". It also links /cite and /ai-models, stated honestly as a pre-registration with no model scored.
  Validation: tsc clean, all 12 test suites pass, build exit 0, and all 15 URLs in llms.txt exist in the build.
• Still uncommitted and undeployed, together with Iteration 10. [DECISION C covers both]
• New decision: should llms.txt and the site say we are not affiliated with compassionbench.com, a live, similarly named site that ranks AI models? I held this back because it names a third party.
• Next loop: the coverage dashboard if you're comfortable publishing the "never individually assessed" share (61.7%). Otherwise the measured-vs-published status ladder.

────────────────────────────────────────
ADDENDUM 2 — deploy, grant request, Iteration 12
────────────────────────────────────────
• Iterations 10–11 are committed and deployed, and I verified them on the live site (deploy run 34901047499, all 4 jobs green).
• Grant request drafted: docs/GRANT_REQUEST_2026-09-14.md. It asks for $250,000/yr: Operations $60k, Research $85k, Expansion $45k, Training $30k, fiscal-sponsor fee $20k, contingency $10k. It includes 16 items for you to supply and 14 decisions. Not committed.
• Iteration 12 (validated, NOT yet committed or deployed): public pages hand-typed "1,156 entities / seven indexes / 21 states / 50 robotics labs". The data says 1,325 / 8 / 51 / 92. All of these now come from the data, Universities was added where four lists left it out, and a test blocks reintroduction. [DECISION: approve commit + deploy]
• Due next: meta-review of the improvement loop (3 loops done), then the coverage dashboard, pending your OK to publish the 61.7% share.

────────────────────────────────────────
ADDENDUM 3: DECISION PACKET (reply by number: yes / no / alternative)
────────────────────────────────────────
Consolidated from the loop meta-review (docs/META_REVIEW_2026-09-14_ITER10-12.md §9). Each item has a recommended default.

1. Commit + deploy Iteration 12, files named below only. Default: YES.
   site/package.json, site/scripts/test-no-stale-counts.mjs, site/src/data/entityCount.ts,
   site/src/app/{api-access,data,indexes,media,page,pricing,purchase-research,score-watch}/…page.tsx (home = site/src/app/page.tsx),
   site/src/app/nonprofit-alt/{page,indexes/page,research/page,support/page}.tsx,
   site/src/app/updates/special/page.tsx, site/src/app/updates/special/[slug]/page.tsx,
   site/src/components/charts/{BandDistributionBar,ChartFrame}.tsx, site/src/components/index/{IndexHero,IndexPageCharts}.tsx,
   site/src/components/updates/briefing/{CompletionBlock,DailyBriefingHeader}.tsx,
   plus CHANGELOG.md, ITERATION_LOG.md, IMPROVEMENT_BACKLOG.md, SYSTEM_HEALTH.md, docs/DEFECT_CLASS_REGISTRY.md, docs/META_REVIEW_2026-09-14_ITER10-12.md, this briefing file.
   EXCLUDED: build-timestamp JSON/manifests, America-at-250 files, research/entity-records-dryrun.json, .bak files, .claude/settings.local.json, grant docs (separate item 11).
2. Uncommitted rewrite of the published America-at-250 briefing (made ~2026-09-03; scope and cohort summary rewritten, sections restructured). Default: HOLD. If it's a correction, publish it as a dated correction note, not a silent rewrite (AUTONOMY §1c). Otherwise discard.
3. Score-Watch (RISK-014; its host does not resolve). Default: pause sales now and hide the badge widget. Check Gumroad for purchases. Deploy the Worker and DNS and do one real test purchase before re-enabling.
4. Publish the "never individually assessed" share on /methodology, generated from data, with its definition and date. Currently 61.7%. Default: YES.
5. Product-separation waivers expire 2026-12-09 (all builds fail from 12-10). Default: extend now on staggered dates and record the decision. If there's no decision by 2026-11-09, the warning tooling becomes the forced next loop.
6. Research cadence floor: at least 5 validated cycles per 7 days, ahead of CB-MODEL build work, with one isolated session per cycle. Default: YES.
7. Approval provenance: branch protection on main, and founder-approved PRs for site/src/data/indexes/** and research/change-proposals/**. Default: YES.
8. Approval throughput: batch commit/deploy approvals once a day, by named file list. Default: YES. A standing approval for no-data, no-score changes that pass all gates would change AUTONOMY §1b, so it's your call only.
9. Refresh CLAUDE.md data notes. They still say "21 of 51 states", "Robotics 50", "193 of 207" and "7 JSON files", and agents read them. Default: replace them with a pointer to entityCount.ts and build-manifest.json (you edit it, or approve my edit).
10. Delete the two .bak files, and run future loops on improve/<date>-<item> branches. Default: YES.
11. Commit the grant request (docs/GRANT_REQUEST_2026-09-14.md) and the correction note on the old grant draft. Default: YES (docs only).

IN PROGRESS NOW: Iteration 13, a lint rule that stops a briefing saying a score "falls" or "rises" when no score change was applied. It applies to briefings dated 2026-09-15 onward; past briefings stay as written. The two live cases are Portugal 07-30 and Hong Kong 09-14. Its files don't overlap Iteration 12's.

────────────────────────────────────────
ADDENDUM 4: Iteration 13 done (validated, NOT committed)
────────────────────────────────────────
• From briefings dated 2026-09-15 onward, the build rejects headlines and summaries that say a score "falls" or "rises" when no change was applied, unless the sentence says the change is proposed or pending. This has already been published in at least 5 cycles: Taipei, Portugal, Spain/Abbott, four entities on 09-01, and Hong Kong. Past briefings are not edited.
• Honest note: the first two versions failed my validation. They would have blocked accurate sentences like "death toll rose to 45". The final version passes 99 tests built from real briefing sentences. Re-run over past briefings, it flags 19 real errors and 2 borderline sentences.
• Effect once deployed: if the nightly writer breaks the rule, that night's build fails instead of publishing the error. That's intended. The writer's instructions now include the rule.
• 1b. [DECISION] Commit + deploy Iteration 13, these files only: site/scripts/lib/lint-rules.mjs, site/scripts/lint-daily-briefings.mjs, site/scripts/test-lint-briefings.mjs, .claude/agents/overnight-digest.md, docs/DAILY_BRIEFING_SCHEMA.md, plus the log files. Default: YES.
• Implementation is now paused: two validated iterations are waiting for your commit approval, the most our uncommitted-work limit allows.

────────────────────────────────────────
ADDENDUM 5 (2026-09-15): governance pass while implementation is paused
────────────────────────────────────────
• SYSTEM_HEALTH.md is now a measured snapshot. It had contradicted the repo in 6+ places, e.g. "auto-deploy broken" (it has 9 successes in a row), "12,750 index checks" (now 85,401, 0 errors), and "21 of 51 states".
• Updated both the coordinator and meta-coordinator agent specs with the adopted loop rules: scoring v2, the uncommitted-work limit and the verification checklist.
• No site, data, score or research changes. Nothing committed.
• Still waiting on you: packet items 1 and 1b (commit Iterations 12 and 13 by named files), 2 (hold the America-at-250 rewrite), and 3 (pause Score-Watch). Once 1 and 1b are approved, the next loop is the entity-records test and slug-collision check (score 19).

────────────────────────────────────────
ADDENDUM 6 (2026-09-15): two findings from today's research preflight
────────────────────────────────────────
• LIVE ERROR (new RISK-023): 20 Fortune 500 company names show raw HTML code on the site, e.g. "Procter &amp; Gamble" in the page title and heading, and "AT&amp;T", "Johnson &amp; Johnson" and "Macy&#x27;s" on /fortune-500. It's been there since the April migration and affects 143 pages. Each company has three different slugs, and the URL people would guess (/company/procter-gamble) redirects to a 404 page.
  Fix plan with exact slugs and redirects: docs/REMEDIATION_RISK-023_ENCODED_NAMES_2026-09-15.md. No scores change.
  12. [DECISION] Approve the rename and slug migration. Default: YES, using the site's slug rule. Your choice for three awkward slugs: "atandt / sandt-bancorp / wandt-offshore" or "at-and-t / s-and-t-bancorp / w-and-t-offshore".
• FALSE ALARM: the research-tracking gate's 22 "no report" failures are all false. The evidence exists under an older naming convention. The research baseline is sound, so I started today's scan. I'll fix the gate once commits unblock implementation.
• In progress: the 2026-09-15 evidence scan (research only, nothing published). I'll decide on assessments after its output passes validation.

────────────────────────────────────────
ADDENDUM 7 (2026-09-15): research cycle complete; your commit instruction
────────────────────────────────────────
• Research cycle 2026-09-15: all 1,329 entities scanned; 14 assessed; 1 proposal; 10 confirmations; 2 band crossings withheld as placeholder rounding. No published score changed.
• 13. [DECISION] Wellington (global cities): proposed 83.0 → 71.3, Exemplary → Established. The evidence is strong: a government-commissioned review, released 2 September, found "longstanding weaknesses in governance, accountability…". But most of the drop is the formula, not the finding. The dimension average moves only 4.00 → 3.85, which is −3.75. The integration bonus disappears once five dimensions dip below 4.0, which is −8.0. This is the RISK-019 cliff. Options: approve as filed, hold pending a decision on the formula cliff, or ask for a re-assessment.
• Correction published in the briefing: the 09-14 research scan wrongly discarded a real Lesotho finding. Two different US trade laws had been confused.
• Anthropic conflict of interest: the assessor is built by Anthropic, and it confirmed Anthropic's score with one pre-committed change. Worth a human look.
• Briefing accuracy: before committing, I found and fixed 12 errors in the drafted public briefing that all the automated checks had passed. Examples: "Oracle at the bottom of the benchmark" (120 entities score lower), and a wrong description of how the bonus works. This is the second cycle in a row, so an automated claim check is now required work (defect class DC-04).
• Per your instruction ("commit and push for manual deployment by me"): everything goes on branch release/2026-09-15, NOT main, so the automatic deploy does not fire. Held back and not committed: the America-at-250 rewrite, the stale entity-records dry run, build-timestamp churn, the .bak files, and local settings.

────────────────────────────────────────
ADDENDUM 8 (2026-09-16): acting on "approve all and fix all"
────────────────────────────────────────
DONE AND VERIFIED
• Iteration 14: the entity-records test (19,687 checks) now runs in npm test, and a new slug collision can no longer reach production. I proved it by planting a 17th collision. CI runs npm test before every deploy, so both guards are real. The 16 existing collisions are frozen in a list that can only shrink.
• Waiver cliff (RISK-015) fixed: all six waivers expired on 2026-12-09, which would have failed every build from 12-10. Now staggered: 2026-11-16, 11-30, 12-14, 2027-01-15, 01-29, 02-12, earliest for the clearest cases. Schema test 17/17 and the validator pass.
• CLAUDE.md data notes corrected. They told every agent "21 of 51 states" and "Robotics 50"; now 51 and 92, with 8 indexes and 1,325 entities, plus an instruction to import the counts rather than type them.
• Your approvals recorded as dated decisions D-31 to D-37, including the slug convention and the Wellington approval, with its provenance caveat stated plainly.
• America-at-250: the unrecorded rewrite is preserved as a patch (research/held-changes/) and stays uncommitted. Republishing it properly as a dated correction is now a backlog item.

IN FLIGHT
• Wellington score being applied; Score-Watch being paused; the coverage figure being generated and published on /methodology.
• Next after those: the 20-company rename and slug migration.

NEEDS YOU: one command
• I could not set branch protection on main — changing repository settings needs a permission this session doesn't have. Run this yourself if you want it (it requires a pull request but keeps your admin bypass, so your manual deploy still works):

  gh api -X PUT repos/Klingdom/applied-compassion-benchmark/branches/main/protection \
    -H "Accept: application/vnd.github+json" \
    -f "required_status_checks=null" -F "enforce_admins=false" \
    -F "required_pull_request_reviews[required_approving_review_count]=0" \
    -f "restrictions=null" -F "required_linear_history=true" \
    -F "allow_force_pushes=false" -F "allow_deletions=false"

• Also still yours: deleting the two .bak files in research/ (a permission I don't have), and checking Gumroad for any Score-Watch purchase that went unfulfilled.

— Compassion Benchmark coordinator
Sources: git log 09-07→09-14; docs/PRFAQ_AI_MODEL_BENCHMARK_AND_CONTINUOUS_RESEARCH_2026-09-14.md; RISKS.md; research/digests/2026-09-14.md; ITERATION_LOG.md Iteration 10.
