services:
  web:
    build: .
    container_name: compassion-benchmark
    ports:
      - "8080:80"
    networks:
      - proxy-net
    restart: unless-stopped

networks:
  proxy-net:
    external: true
