certbot/
nginx-active.conf

# Pipeline runtime state (297KB rewritten each run, bad for git history)
# The scan files already record what was processed each night for auditability
research/rotation-state.json
research/rotation-state.json.tmp

# Build artifacts
site/.next/
site/out/

# Pipeline temp files
research/logs/
*.tmp

# Raw Nginx access/error logs (persisted host volume — see docker-compose.yml).
# Contains visitor IPs; large; never committed. Only the derived, IP-agnostic
# scan summaries under research/aeo-crawler-logs/ are tracked in git.
logs/
