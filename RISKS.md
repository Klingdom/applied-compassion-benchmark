# Risk Register

Live risks for Compassion Benchmark, each grounded in a verifiable figure or artifact in this
repository. Adapted in structure from the 6S Success operating system; populated entirely from
this repo's own data. See `docs/OPERATING_SYSTEM_ADAPTATION.md`, `INCIDENTS.md`.

Likelihood/Impact are qualitative (Low/Medium/High) — this repo has no incident-frequency data
sufficient to justify numeric scoring.

---

| ID | Risk | Likelihood | Impact | Evidence | Mitigation | Owner | Status |
|---|---|---|---|---|---|---|---|
| RISK-001 | **~59% of published entities sit on placeholder scores, not measurements.** *(Corrected 2026-09-14: the ~59% is the 759 of 1,289 never individually assessed; the 834 below is the identical-vector cluster count, 64.7%. The same mispairing appears in `docs/GRANT_PROPOSAL_2026-09.md` lines 27 and 408. A different measure on 2026-09-14: 820 of 1,329 entities, 61.7%, have `last_assessed` null in `research/rotation-state.json`.)* 834 of 1,289 published entities carry an 8-dimension vector byte-identical to at least two others in the same index (a never-individually-assessed uniform seed); of those, 75 already have a pending proposal awaiting approval, leaving 759 genuinely un-assessed. Highest-severity instances are placeholders published in the **Exemplary** and **Established** bands — the two highest labels the benchmark issues — on values that were never a measurement of anything. | High (structural, not episodic) | High — the benchmark's core claim is measurement; a majority-placeholder corpus undercuts it directly | `research/SEED_INVENTORY_2026-08-20.md` §Headline; 7 completed de-seeding studies show seed error scales monotonically with seed height and can run in either direction (seed is a floor for a state, a ceiling for a small company) | De-seeding programme in progress; `research/SEED_INVENTORY_2026-08-20.md` §6 sets a priority order (fix the 60.9 band-boundary ambiguity first, then the 4 fortune-500 entities at seed 92.4, then countries at 83.0) | benchmark-research | Open, in progress |
| RISK-002 | **50 approved proposals are held, not applied** — published scores knowingly diverge from the benchmark's own most recent assessed values. 37 held on explicit assessor self-veto (rationale contained language such as "DO NOT APPLY" or "recommend human review before any application" that a bare `status: approved` flag does not override); 13 held on entity-record grounds (corporate-status questions — mergers, defunct status — that must resolve before a score can attach to a stable identity). | Medium (accumulates each cycle; already large) | Medium-High — every day these sit unapplied is a day the public site shows a score the benchmark's own process has already superseded internally | `research/APPLIED_CHANGES.md` line 723: "several public briefings...describe some of these proposals as pending, not applied...(unchanged, and correctly still framed as pending, for the 37 held entities and the 13 entity-record holds)"; corroborated in `research/PENDING_CHANGES.md`'s 2026-08-20 entry | Self-veto holds require a fresh, non-veto-conflicted re-assessment, not an override of the veto; entity-record holds require the corporate-status question resolved first (see RISK-003) | coordinator / founder | Open |
| RISK-003 | **12+ entity-currency defects**: companies ranked while defunct, renamed, acquired, hollowed out, or duplicated. Confirmed cases: Rethink Robotics (defunct, ranked "Established" — the second-highest band — 11 months after its 2025-09-16 closure); Boston Dynamics listed twice within robotics-labs, 45.3 points apart; 1X Technologies/Halodi Robotics — one company, renamed in 2023 — listed twice, 18.9 points apart; five cross-index double-publications (Microsoft AI vs. Microsoft, Amazon AWS AI vs. Amazon, Meta AI vs. Meta Platforms, 1X Technologies vs. its own robotics-labs record, Figure AI vs. its own robotics-labs record — gaps up to 23.1 points for the *same company*); two unresolved corporate-status questions (Adept AI, Inflection AI — both substantially absorbed into acquirers); one country duplicated under two spellings (Sao Tome and Principe / São Tomé and Príncipe); a second country duplicated the same way, **Cabo Verde / Cape Verde** (`countries.json` ranks 25 and 26, identical 62.5 composites and dimension vectors — found 2026-09-14 by the nightly assessor). **RESOLVED 2026-09-14** by founder-directed structural merge (D-08): Cape Verde and the accented São Tomé row removed (countries 193 → 191; rotation-state 1,331 → 1,329, including a hidden `s-o-tom-and-pr-ncipe` orphan in four stores); `/country/cape-verde` 301 → `/country/cabo-verde`. Same day: truncated "Democratic Republic of C" renamed "Democratic Republic of the Congo" with slug `democratic-republic-of-c` pinned. See `research/APPLIED_CHANGES.md` 2026-09-14. Cross-index slug collisions are tracked separately as RISK-017. | Medium | High for the defunct/duplicate cases specifically — a reader cannot reconcile two different scores for what is presented as one entity | `research/INDEX_EXPANSION_SCOPE_2026-08-20.md` §1.1–1.2; `research/assessments/rethink-robotics-2026-08-16.md` | Disposition recommendations already drafted (delist-and-merge for duplicates, dated tombstone for defunct entities, hold-pending-status-check for the ambiguous two) in `research/INDEX_EXPANSION_SCOPE_2026-08-20.md` §1.1–1.2 — **not yet executed** | benchmark-research / coordinator | Open, remediation designed but not applied |
| RISK-004 | **[Updated 2026-09-14] Auto-deploy restored; residual gap is verification, not delivery.** The `Deploy to VPS` workflow failed 53 runs (2026-07-14 → 2026-09-08) and has succeeded 7 consecutive runs since 2026-09-09. Residual risk: the post-deploy verify job asserts freshness only on the latest briefing date and prints — does not assert — sample score values, so a score-only change can deploy stale undetected (D-09's "liveness ≠ currency" lesson still applies). | Low (delivery) / Medium (undetected stale scores) | High — production is the published record | `gh run list --workflow deploy.yml` (60 runs: 53 failure, 7 success; first success 2026-09-09); `.github/workflows/deploy.yml` verify job; INC-001; `docs/prfaq/2026-09-14/reviews/devops-engineer.md` | Make the verify job assert an applied score value the commit expects to change (compare `/data/scores/<slug>.json` composite against the committed index) | devops-engineer / github-manager | Mitigated (delivery) — Open (verification gap) |
| RISK-005 | **Two live scoring conventions for "absence of disclosure" are simultaneously in use within the ai-labs index.** Cerebras Systems (already applied, published at 38.8) was scored under a convention that assigns absence-of-disclosure a raw 3; at least 56 peer entities across five completed de-seeding studies (robotics 60.9: 10, robotics Exemplary: 10, robotics mid/low: 14, ai-labs high-seed: 15, ai-labs low-seed: 7) were scored under a convention that assigns the same condition a raw 2 — the "majority" and "more conservative" choice per the studies' own documentation. Cerebras is not a safe peer anchor for any of the 56 under the other convention, and the −22.1 delta that moved it to 38.8 is not comparable to deltas produced under the 2-convention. | Medium | Medium — affects internal consistency and cross-entity comparability within one index, not the accuracy of any single already-published score in isolation | `research/SEED_CLUSTER_AI_LABS_LOW_2026-08-17.md` §6: "Two conventions are therefore live in the ai-labs index simultaneously, and Cerebras is not a safe peer anchor for any entity in this study or the 15 assessed alongside it"; `research/SEED_CLUSTER_ROBOTICS_MIDLOW_2026-08-17.md` line 73 | Recommended fix stated directly in the source study: re-assess Cerebras under the 2-convention rather than adjusting the 56 peers already scored under it | benchmark-research | Open |
| RISK-006 | **Band-boundary ambiguity at composite 60.9**, undocumented, affecting roughly 30 entities. The methodology's own documentation states bands as `41–60 Functional`, `61–80 Established` — a value of 60.9 falls in the gap between the two ranges as written. `computeCompositeFromDimensions` resolves it to Established in practice, but nothing in the written methodology says so. **Extended 2026-09-14:** boundaries also disagree at exact integers — `getBand()` in `scoring.mjs`/`scoring.ts` is upper-inclusive (60.0 → Functional; `BAND_RANGES` "61-80") while `dimensions.ts` `BANDS`, declared the single source of truth, sets Established `min: 60` ("60–80"). Mauritius and Spain are published at exactly 60.0 as functional. Same class as Iteration 9 Tranche B item B2 (gated: reclassifies published entities). | Medium | Medium-High — this is specifically the seed value carrying the highest concentration of un-de-seeded fortune-500 Established-band placeholders (17 remaining entities), so an undocumented boundary decision is propping up a real published claim | `research/SEED_INVENTORY_2026-08-20.md` §5, "carried as the 'Erie Indemnity band-boundary label gap' since 2026-07-30" | Recommended: fix the boundary table in the methodology documentation *before* de-seeding the 60.9 clusters, so those studies are not scored against an ambiguous target | founder / benchmark-research | Open |
| RISK-007 | **Reputational/legal exposure from publishing scored judgments about named, real institutions.** Cerebras Systems moved −22.1 (Established → Developing band crossing) with **zero adverse evidence located in either direction** — the delta measures absence of public disclosure, not misconduct, and the study that produced it says so explicitly. | Low-Medium (mitigated, see below) | High if the mitigation lapses — a large negative score movement with no adverse finding behind it is the single easiest kind of entry for a scored entity to dispute publicly | `research/PENDING_CHANGES.md` line 24 ("zero adverse evidence located"); `research/PENDING_CHANGES.md` line 9049 ("a dedicated tier-1 thematic search found zero adverse evidence in either direction; the 22.1-point gap measures absence of disclosure, not conduct") | **This risk is actively mitigated, and the mitigation should be named as such rather than assumed:** every SEED-BASELINE-RECALIBRATION proposal is required to carry an explicit caveat distinguishing "no compassion infrastructure was disclosed" from "misconduct was found," and `research/APPLIED_CHANGES.md` records this caveat requirement was applied to Cerebras at time of publication. The mitigation is the caveat discipline itself, not the absence of the underlying risk. | benchmark-research / coordinator | Open, actively mitigated |
| RISK-008 | **`rotation-state.json` gitignore is ineffective and the fix path is unresolved.** The file is listed in `.gitignore` (comment: "bad for git history") but is tracked in git regardless, so the ignore rule has never taken effect. It is the pipeline's staleness baseline for all 1,331 tracked entities. Untracking it without first building a seeding path would leave a fresh clone unable to run the nightly pipeline at all. | Low (only triggers on a specific future cleanup action) | High if triggered — would silently disable staleness-based scan prioritization for the entire benchmark | `.gitignore` lines 6–7 vs. `git ls-files research/rotation-state.json` returning the file; `.claude/agents/github-manager.md` "Repository hygiene" section | **Explicitly not resolved by this document, per instruction.** Flag before any `git rm --cached` on this path; require a seeding/bootstrap script in the same change if untracking proceeds. See `DISASTER-RECOVERY.md` §2. | founder (architectural decision) | Open, deliberately unresolved pending a decision |
| RISK-014 | **Score-Watch (sold as LIVE at $79/yr) and every badge embed depend on a hostname that does not exist.** `api.compassionbenchmark.com` returns NXDOMAIN on public DNS; `worker/wrangler.toml` routes the Worker only on that host; `BadgeEmbedWidget.tsx:28` gives every entity-page visitor a badge URL on it; `worker/README.md` documents the Gumroad webhook on it. Purchases cannot reach the documented webhook. Whether Gumroad's ping URL is configured elsewhere, and whether any purchase went unfulfilled, is unverified. | High (current state) | High — paid customers and third-party embeds; direct trust damage | `nslookup api.compassionbenchmark.com 1.1.1.1` → Non-existent domain (verified by coordinator 2026-09-14); `site/src/data/gumroad.ts:8`; `site/src/app/score-watch/page.tsx:17`; `docs/prfaq/2026-09-14/reviews/security-auditor.md` | Founder: create the DNS record and deploy the Worker route, or pause Score-Watch sales and hide the badge widget until live; check Gumroad sales history and ping-URL configuration | founder / devops-engineer | **Sales paused 2026-09-16** (D-34, founder-approved): `SCORE_WATCH.useGumroad = false`, so every CTA routes to `/contact-sales?product=score-watch` and no purchase link is reachable (coordinator-verified: both remaining call sites are flag-gated); the badge-embed widget renders nothing behind `BADGE_EMBED_AVAILABLE = false`; the pause is disclosed on `/score-watch` and `/pricing`. **Still open:** the host does not resolve, the Worker is undeployed, and whether any past purchase went unfulfilled is unverified — the founder must check Gumroad sales history. Re-enable only after DNS + Worker + one verified end-to-end test purchase |
| RISK-025 | **[Coordinator error, found by Meta-review 4 and verified] `[skip ci]` on a push tip silenced CI for four days.** To honour "manual deployment", the coordinator put `[skip ci]` on the newest commit of four pushes to `main`. GitHub applies that to the **whole push**, not just the deploy step, so build, test and the nginx syntax check never ran either. Verified 2026-09-21: newest CI run is `119f1757` (2026-09-17T16:56Z) with **12 commits pushed since**, none built; Iterations 19 and 21–25 plus three research cycles were never CI-verified; production was last built 2026-09-18T13:53Z by a bare `docker compose build` reporting `git.sha: null`, so the drift was invisible; and `/updates/2026-09-18`, `/updates/2026-09-20` and `/updates/2026-09-21` all **301 to /404** — three committed briefings that readers cannot see. Iteration 21's own post-deploy redirect sweep has therefore never executed. | High (was live for 4 days) | High — no CI gate, and published work silently unpublished | `gh run list`; `git log --oneline 119f1757..HEAD` = 12; live 301s; `/build-manifest.json` `source: "unavailable"`; coordinator-verified 2026-09-21 | **Fixed in the working tree (pending commit):** the `deploy` and `verify` jobs are now gated `if: github.event_name == 'workflow_dispatch'`, so a push always runs build + test + nginx syntax while shipping stays a deliberate human act. `[skip ci]` is no longer needed and must not be used on a push tip (amendment D1) | coordinator | **PARTIALLY MITIGATED — and the cause is now known to be incomplete.** The gating fix is committed and pushed (`9d89d4df`, deliberately **without** `[skip ci]`, so the push was itself the test). **It produced no CI run.** Verified 2026-09-21: `actions/runs?created=>=2026-09-18` returns `total_count: 0` in **any** status including `skipped`; the repo is **public** (so Actions minutes are not the constraint), not archived, not disabled; `actions/permissions` is `enabled: true, allowed_actions: all`; the workflow is `state: active`; and the file on `main` carries both dispatch guards and the `on: push: branches: [main]` trigger. So `[skip ci]` explains the three earlier pushes (GitHub records no run at all for a skipped push) but **not** this one. **A second, unidentified cause is suppressing push-triggered runs** — needs a look at the repository Actions tab, which is where GitHub surfaces banner-level suppression messages an API cannot see. Under amendment D1 this loop is **not closed**: there is no CI run whose headSha is its commit. |
| RISK-024 | **A repo script designed for unattended cron pushes to `main`.** `scripts/nightly-pipeline.sh` is documented as "designed for unattended execution via cron" and its stage 7 runs `git push origin main`, while `AUTONOMY.md` §1b makes every commit and push a founder action. It is not currently scheduled (no crontab in the repo), so exposure today is latent rather than live — but it is one `crontab -e` away from nightly rule-bypass, and its failure webhook is optional (`if [ -n "${NIGHTLY_WEBHOOK_URL:-}" ]`), so a bad night could be silent. | Medium (latent) | High — unreviewed commits on the production branch | `scripts/nightly-pipeline.sh:5,15,212-216,86`; `AUTONOMY.md` §1b; coordinator-verified 2026-09-20 | Remove the push, or gate it behind an `ALLOW_PUSH` flag that is off by default (backlog AUT-3); make the failure channel mandatory (AUT-5) | coordinator → founder | **OPEN** |
| RISK-015 | **[Updated 2026-09-17] Every build and deploy fails from 2026-11-17 unless D-13 is decided — now with 30 days' notice.** The single 2026-12-09 cliff was staggered on 2026-09-16 (D-37), so the earliest expiry is `D13-figure` 2026-11-16; Iteration 22 warns from T-30 and reports `PASS WITH WAIVERS` distinctly, and `docs/D-13_DETERMINATIONS_DRAFT_2026-09-17.md` holds a drafted disposition for all six. Original entry: **Every build and deploy will fail from 2026-12-10.** All 6 waivers in `site/scripts/product-separation-waivers.json` expire 2026-12-09; `validate-product-separation.mjs` compares against the current date and runs inside `npm run build`. | High (dated; certain unless acted on) | High — blocks all publication | `site/scripts/product-separation-waivers.json` (six `"expires": "2026-12-09"`); `site/scripts/validate-product-separation.mjs:225` uses `new Date()`; `docs/prfaq/2026-09-14/reviews/system-architect.md` (verified by coordinator 2026-09-14) | Ratify or reject D-13 and remediate the 6 duplicate-publication cases, or consciously extend the waivers with a recorded decision, before 2026-12-09 | founder | **Mitigated 2026-09-16** (D-37, founder-approved): the single 2026-12-09 cliff is gone — expiries now staggered figure 11-16 · 1x-technologies 11-30 · boston-dynamics-spot-demo 12-14 · amazon 2027-01-15 · meta 2027-01-29 · microsoft 2027-02-12; `test-separation-waivers` 17/17 and `validate-product-separation` PASS (6 waived) on the new dates. Open: D-13 itself is still unratified, so each date will force its own decision |
| RISK-016 | **The nightly research pipeline has never run unattended.** Every research cycle to date was run manually in an interactive session, so cadence depends on founder availability and on one session's 2,000-search cap (INC-008). Cadence fell from daily (through 2026-07-31) to 7 catch-up cycles in August–September. | High (current state) | High — freshness is the benchmark's core promise | All 94 research commits carry the founder identity; 0 of 426 commits fall between 02:00 and 05:59; the VPS identity configured in `scripts/vps-bootstrap.sh:118-119` appears on no commit (verified by coordinator 2026-09-14); `docs/prfaq/2026-09-14/reviews/meta-coordinator.md`; `devops-engineer.md` | Decide the execution model: one isolated session per cycle with a search-budget preflight; do not enable unattended push-to-main until briefing claims are checked against sources (RISK-020) | founder / devops-engineer | Open |
| RISK-017 | **Cross-index slug collisions overwrite public score files, entity records and badges.** 18 collisions in the flat `/data/scores` and `entity-records` stores; the last-written index wins and `export-public-data.mjs` only warns. The country Singapore's score file serves the global city. Phoenix fixed 2026-09-14 (founder-directed; pinned `phoenix-global-cities`, 301 from `/city/phoenix`). **Singapore fixed 2026-09-16 (Iteration 19):** pinned `singapore-global-cities`, 301 from `/city/singapore`; the bare slug now serves the country (62.2) instead of the city (56.2). **15 remain** — 12 US cities in global-cities, `washington-dc` (which needs a both-sides pin: rotation-state has no bare key for it), and the 2 labs. 1X Technologies and Figure AI are a D-13 index-ownership question, not a naming fix — the coordinator pinned them in Iteration 19 and **reverted**, because D-13's hard constraint is that no entity may hold more than one published composite, so a slug per composite entrenches the duplicate publication the decision exists to remove. **Register discrepancy to resolve:** this row counts "15 US cities plus Singapore" while `site/scripts/known-collisions.json` counted 13 cities + Singapore + 2 labs; the two disagree about which 16 existed. | High (current state) | Medium-High — wrong entity served to badges and data consumers | `/data/scores/singapore.json` has indexSlug global-cities (verified by coordinator 2026-09-14); `site/scripts/export-public-data.mjs` collision WARN; rotation-state already uses index-suffixed keys such as `phoenix-global-cities` | Pin index-suffixed slugs on the colliding rows with 301s for moved pages (Georgia and Phoenix precedent), then make any cross-index collision fail the build | coordinator / score-updater / frontend-engineer | Open — Phoenix resolved |
| RISK-018 | **[Updated 2026-09-17 — live, now gated and ratcheted, not fixed]** Confirmed on production: `/city/sao-paulo` 200 but `/data/scores/sao-paulo.json` 301 -> `/404` (the file is `s-o-paulo.json`); **14 of 16** non-ASCII names diverge, listed in `site/scripts/known-slug-divergences.json` (shrink-only, enforced by `test:slug-conventions`, It. 23). The 12 duplicate slug implementations that caused the drift are now one module. Remediation is backlog **RS-4b** (fold + 301s + S9 re-derivation), founder-gated. Original entry: **Accented entity names resolve to different slugs in pages and data.** Two slugify implementations disagree on accent folding: pages use folded slugs (`/city/sao-paulo`) while score files, entity records and rotation keys use unfolded ones (`s-o-paulo`). 13 entities affected: 12 global cities, plus Côte d'Ivoire, which has neither a record nor a score file. | High (current state) | Medium — broken badge and data URLs; orphaned records (the class fixed for 26 entities in commit 70f81dc2) | `/data/scores/sao-paulo.json` → 404 and `/data/scores/s-o-paulo.json` → 200 (verified by coordinator 2026-09-14); `site/src/lib/slugify.ts` folds accents, the export and record scripts do not | One shared slug function for pages, export and records; migrate the 13 with redirects; add a test that every index row has a record and a score file at its page slug | frontend-engineer / backend-engineer | **Open — one face closed 2026-09-16.** Entity history no longer breaks when a slug changes: `build-entity-history.mjs` resolves each dated briefing's reference to the current catalogue slug (exact → unambiguous decoded name → derived aliases) and merges old and new events, so renames stop orphaning history. Verified live after deploy 35118662309: four renamed entities' `/history` pages returned from 301 → `/404` to 200. The underlying two-slug-conventions problem remains (13 accented-slug mismatches, 16 cross-index collisions), and 5 briefing references still resolve to no published entity |
| RISK-019 | **A published methodology claim is contradicted by the canonical formula.** `/ai-models/methodology` says a balanced profile scores higher than a spiky one with the same average; under `scoring.mjs` a [1,1,1,1,5,5,5,5] profile scores 51.5 against 50.0 for uniform 3.0, and [5,5,5,5,5,1,1,1] scores 65.5 against 62.5 for uniform 3.5. The integration premium also creates a cliff: all dimensions at 3.99 → 74.8 (Established), at 4.0 → 85.0 (Exemplary). Related record-accuracy issue: D-30's "BYO scoring emits no composite" holds only for runs that include AI-judge items or unscored dimensions. | Certain (current state) | High — methodology credibility; the same formula is reused for models | `site/src/app/ai-models/methodology/page.tsx:31-32`; values computed with `computeCompositeFromDimensions` (verified by coordinator 2026-09-14); `site/src/components/model-benchmark/EvaluationScorer.tsx` ~285–309; `docs/prfaq/2026-09-14/reviews/benchmark-research.md` | Short term: correct the page copy to describe the formula as it is. Decision needed: whether to change the premium (Tranche B; re-scores the catalogue). **2026-09-14 (Iteration 10): short-term copy fix done, uncommitted** — the false FAQ sentence now describes the formula as it is (verified against `scoring.mjs`); the 3.99→4.0 cliff and the premium-change decision remain open. | founder / benchmark-research | Open — copy fixed (pending deploy); cliff/premium decision open |
| RISK-020 | **Factual errors in public daily briefings pass every automated gate.** On 2026-09-14 the digest wrote four errors into the public briefing (a wrong embassy fact, an inverted causal claim, an implied correction that never happened, an invented process claim) that passed `lint-daily-briefings` and `validate-daily-briefings` and were caught only by coordinator review. | High (observed 2026-09-14) | High — published under the benchmark's name | Commit cef937df message; `site/src/data/updates/daily/2026-09-14.json` (verified by coordinator 2026-09-14) | Add a claim-to-source check comparing briefing text against the cycle's assessment files; keep human review before publication until it exists | qa-engineer / overnight-digest owner | Open |
| RISK-021 | **Approval provenance is not verifiable.** Score changes are authorised by `status: "approved"` and `reviewed_by: "founder"` fields in proposal JSON that any agent can write. On 2026-09-14 the coordinator recorded the Hong Kong approval on the founder's explicit in-session instruction; nothing mechanically distinguishes that from an agent-authored approval. | Medium | High — the independence and human-authority claims rest on this trail | `research/change-proposals/hong-kong-2026-09-14.json`; AUTONOMY.md §5 R5; `docs/prfaq/2026-09-14/reviews/security-auditor.md` | Signed approvals or a founder-only approval channel, plus branch protection on main; record which actor wrote each approval field | founder / security | **Open — partially addressed 2026-09-16.** D-31 requires dated branches and named pathspecs; the Wellington apply carries an explicit `approval_note` naming the founder's in-session instruction and stating that the provenance is not mechanically verifiable. Branch protection on `main` was attempted and **blocked — changing repository settings needs a permission this session lacks**; the exact `gh api` command is in `docs/founder-briefings/2026-09-14.md` addendum 8 for the founder to run |
| RISK-022 | **Public admin surface and missing security headers.** `/u/login` serves a public Umami analytics login; responses carry X-Frame-Options, X-Content-Type-Options and Referrer-Policy but no Strict-Transport-Security and no Content-Security-Policy. | Medium | Medium | `curl https://compassionbenchmark.com/u/login` → 200, page title "Login – Umami";`curl -I https://compassionbenchmark.com/` headers (verified by coordinator 2026-09-14) | Restrict `/u/` to an allowlist or a separate host; add HSTS and a CSP in nginx-ssl.conf | devops-engineer | Open |
| RISK-023 | **20 Fortune 500 company names are published with raw HTML entities**, visible to readers — "Procter &amp; Gamble", "Johnson &amp; Johnson", "AT&amp;T", "Macy&#x27;s" and 16 more — in page titles, H1s, the /fortune-500 ranking, peer lists on 143 built pages, score files and entity records. Each affected company also has three disagreeing slugs: page (`procter-andamp-gamble`), score file / rotation key / entity record (`procter-amp-gamble`), and the natural clean URL (`/company/procter-gamble`), which 301-redirects to `/404`. Present since the initial migration (commit `a60208d9`, 2026-04-14). | Certain (current state) | Medium-High — credibility on some of the most-searched companies; broken natural URLs and badge/data links | `site/src/data/indexes/fortune-500.json` (20 names with `&amp;`/`&#x27;`); live `https://compassionbenchmark.com/company/procter-andamp-gamble` 200 with `<title>Procter &amp;amp; Gamble …` and `/company/procter-gamble` → 301 `/404` (verified by coordinator 2026-09-15); `site/out/data/scores/procter-amp-gamble.json` name "Procter &amp; Gamble"; 20 `site/src/data/entity-records` files | Decode the 20 names at the data source; migrate to one clean slug per entity with 301s from both old slugs; add a validator that fails on HTML entities in any published name. Name/slug changes are founder-gated (AUTONOMY §1b); the validator is agent-doable | founder / coordinator | **Remediated 2026-09-16** (D-35, founder-approved), pending build/deploy verification: all 20 names decoded and an explicit `slug` pinned on each row (`procter-and-gamble`, `johnson-and-johnson`, `at-and-t`, `macys`, …), honoured by both the page router and the data export; the 20 entity records renamed and rotation keys rekeyed with every other field untouched; 60 redirect lines added to each of `nginx.conf` and `nginx-ssl.conf` (old page slug, natural-guess slug, old score-file URL); `validate-indexes` check 17 now fails on any HTML entity in a published name, proven by a planted probe and wired into `npm run test` as `test:encoded-names`. Coordinator-verified: **0 encoded names across all 8 indexes** (was 20), 20 pinned slugs present, no composite/band/rank/dimension changed (scripted diff vs HEAD), `validate-indexes` 0 errors, `test-entity-records` 19,687/0, collision ratchet unchanged at 16/0/0. Residual: 3 rotation-state validator false failures from reports filed under the pre-decode slugs — fixed in the validator (RS-1), not by renaming historical reports |
